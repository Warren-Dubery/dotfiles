" 

" as recommended to protect from unexpected things happening
set nocompatible


" don't wrap text
set nowrap

" Syntax highlighting
syntax on

" Use bell image rather than bell sound
set visualbell

" Enable mouse for all modes
set mouse=a

" Displays lines numbers on left-side
set number
